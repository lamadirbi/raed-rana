# Vercel KV / Upstash — تُضاف تلقائياً من لوحة Vercel عند ربط قاعدة KV
# محلياً: انسخ إلى .env.local أو نفّذ `vercel env pull .env.development.local`
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=
REDIS_URL=
